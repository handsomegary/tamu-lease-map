# Large GIS/raw data files. Keep these in the repo for runtime use, but do not
# load them into Codex context unless a task specifically needs their contents.
outputs/*.geojson
work/states-10m.json

# Local/generated tool state.
.agents/
.codex/
node_modules/
*.log
*.tmp
